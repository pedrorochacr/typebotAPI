{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Backend TypeScript",
      "program": "${workspaceFolder}/dist/server.js",         // ou o main do seu backend
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "sourceMaps": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
